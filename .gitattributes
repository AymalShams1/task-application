# Normalise line endings so shell scripts stay LF inside Linux containers.
* text=auto eol=lf
*.sh    text eol=lf
mvnw    text eol=lf
*.cmd   text eol=crlf
*.bat   text eol=crlf

# Treat lockfiles as generated so diffs stay collapsed on GitHub.
package-lock.json linguist-generated=true
